# ModelPort 使用手册页面规划

## 1. 目标

在登录后的全局顶部栏中，消息通知铃铛左侧增加“使用手册”入口，提供从软件下载、创建密钥、中转站配置到故障排查的一站式上手路径。

核心目标不是简单堆放文章，而是让新用户完成这条闭环：

> 选择客户端 → 下载客户端 → 创建 API Key → 选择分组 → 自动或手动配置 → 发起验证请求 → 能看懂用量与错误

首期成功标准：普通用户不联系客服，也能在 5–10 分钟内完成 macOS / Windows Codex 桌面版或 CC Switch 的首次配置并发出成功请求。

## 2. 当前基础与结论

### 2.1 已有能力

- `frontend/src/components/layout/AppHeader.vue` 已有顶部“文档”外链，但受 `doc_url` 控制，目前排在通知铃铛右侧，且小屏直接隐藏。
- `frontend/src/views/user/CustomPageView.vue` 已支持 Markdown、安全净化、文章目录、标题定位、代码复制和图片资源。
- 后端 `backend/internal/handler/page_handler.go` 已支持读取 `data/pages/<slug>.md`，但页面必须依附 `custom_menu_items`，且当前只有读取/列举接口，没有后台编辑或上传能力。
- `frontend/src/components/keys/UseKeyModal.vue` 已包含 Codex、Claude Code、Gemini CLI、Grok CLI、OpenCode 等配置生成逻辑。
- `frontend/src/components/keys/CcSwitchImportModal.vue` 和 `frontend/src/utils/ccswitchImport.ts` 已提供 CC Switch 一键导入、模型目录读取、余额查询脚本和手动配置回退。

### 2.2 方案选择

推荐采用“内置手册壳 + 版本化教程内容 + 复用现有配置动作”的方案：

- 新增固定路由 `/guide`，不依赖管理员是否配置 `doc_url` 或自定义菜单。
- 手册目录与交互由专用 Vue 页面负责；教程正文首期随代码版本发布，保证教程与当前功能一致。
- 配置示例不要复制一份写死在文章里，应复用现有配置生成器，或引导用户进入“API 密钥 → 使用密钥/CC Switch 导入”。
- 现有 `doc_url` 保留为“更多文档/外部文档”，放进手册页，不再占用顶部主要入口。
- 后续如确实需要免部署更新教程，再增加受审计的后台 Markdown 编辑和发布能力。

不推荐首期直接使用 `/custom/:id` 作为使用手册：它依赖自定义菜单 ID 和服务器文件，部署环境容易出现“菜单有了但 Markdown 文件没挂载”或反向情况，也难以承载用户分组、Key 和一键导入等动态操作。

## 3. 全局入口设计

### 3.1 桌面端

顶部栏顺序调整为：

> 使用手册 → 消息通知 → 模型广场 → 语言 → 订阅 → 余额 → 用户菜单

入口规范：

- 图标：现有 `book` 图标。
- 文案：中文“使用手册”，英文“Guide”。
- 点击：站内跳转 `/guide`，不新开标签页。
- 激活态：路由位于 `/guide` 或 `/guide/*` 时使用主色浅底。
- 可访问性：提供 `aria-label`、键盘焦点样式和至少 40×40 px 的点击区域。
- 可见性：所有已登录用户显示，管理员和普通用户看到同一入口；文章内部再根据角色隐藏不相关内容。

### 3.2 移动端

- 顶部保留书本图标，隐藏文字，不能像当前文档链接一样整个入口消失。
- 用户下拉菜单中再增加一条“使用手册”，作为第二入口。
- 手册页左侧目录改为抽屉；搜索框保持在页面顶部。

### 3.3 与现有外部文档的关系

- 如果管理员配置了 `doc_url`，手册首页底部显示“更多官方文档”外链。
- 如果未配置，不显示该卡片。
- 外链统一使用 `target="_blank"`、`rel="noopener noreferrer"`。

## 4. 页面信息架构

### 4.1 手册首页 `/guide`

页面顶部：

- 标题“使用手册”。
- 副标题“从安装客户端到成功发出第一条请求”。
- 全文搜索：支持教程标题、关键词和常见错误码。
- “最近更新”提示：显示手册版本或站点版本，不显示容易过期的硬编码发布日期。

第一屏提供三个明确入口：

1. **第一次使用**：选择客户端并走完整快速开始。
2. **已有客户端，开始配置**：跳转创建 Key、分组选择和配置。
3. **遇到问题**：直接进入错误排查。

### 4.2 左侧目录

#### A. 快速开始

- 平台与客户端怎么选
- 注册、充值和创建 API Key
- 分组是什么，如何选择
- 5 分钟发出第一条请求

#### B. 客户端下载

- Codex 桌面版（macOS）下载与安装
- Codex 桌面版（Windows）下载与安装
- Codex CLI（进阶，可选）
- CC Switch 下载与安装
- Claude Code 下载入口
- Gemini CLI、OpenCode、Grok CLI 下载入口

#### C. 中转站配置

- CC Switch 一键导入
- CC Switch 手动配置
- Codex 手动配置
- Claude Code 环境变量与 `settings.json`
- Gemini CLI 配置
- OpenCode 配置
- Grok CLI 配置

#### D. 密钥、模型与计费

- 创建、禁用和删除 API Key
- API 地址、模型 ID 和分组的关系
- 流式请求、Token 与缓存计费
- 额度、并发、限速和有效期
- 如何查看使用记录与余额
- 密钥安全和泄漏处理

#### E. 故障排查

- 401 / Missing API key
- 403 / Upstream access forbidden
- 404 / model not found
- 429 / 限速或余额不足
- 502 / Bad Gateway
- `stream disconnected before response completed`
- CC Switch 点击导入无反应
- Windows 无法唤起 `ccswitch://`
- 配置导入后客户端仍使用旧供应商
- 首字延迟高、请求长时间无响应

#### F. 常见问题

- 是否必须使用 CC Switch
- 配置后为什么要完全重启客户端
- 同一个 Key 能否多设备使用
- 为什么网页计费和客户端 Token 显示不同
- 如何联系支持，以及提交哪些脱敏信息

## 5. 单篇教程模板

每篇教程使用统一结构，降低用户理解成本：

1. **适用场景**：适合哪个客户端、分组和操作系统。
2. **预计用时**：例如 3 分钟。
3. **开始前准备**：账号、可用 Key、余额、客户端版本。
4. **操作系统标签**：Codex 主教程只提供 macOS / Windows；其他客户端按实际支持范围提供对应系统标签。
5. **分步操作**：每步包含动作、截图、预期结果。
6. **复制配置**：示例使用 `<YOUR_API_KEY>`，真实 Key 只通过现有密钥弹窗注入。
7. **立即验证**：提供一条最小请求或“发送测试消息”的检查方法。
8. **成功标准**：用户应看到的模型、输出或状态。
9. **常见错误**：本步骤最容易遇到的 2–4 个错误和解决办法。
10. **下一步**：链接到用量、模型选择或故障排查。

## 6. 三篇核心教程规格

### 6.1 Codex 桌面版下载教程（macOS / Windows）

内容范围：

- 页面主标题使用“Codex 桌面版”，正文首次出现时说明：当前官方产品入口是“ChatGPT 桌面应用”，安装后在应用内选择 Codex。
- 首屏直接提供两个大按钮：“下载 macOS 版”和“下载 Windows 版”，不再把 CLI、IDE 扩展或 Linux 放在主安装路径中。
- 将“下载官方桌面应用”与“接入 ModelPort 中转”拆成两个连续阶段，避免把本站配置描述成 OpenAI 官方配置。

macOS 教程：

1. 从 OpenAI 官方桌面应用页面下载 macOS 版本；按钮始终指向官方最新地址，不托管或镜像安装包。
2. 安装并首次启动应用，按系统提示完成必要权限授权。
3. 登录 ChatGPT 账号，或在应用支持的情况下选择 API Key 方式；随后在产品选择器中进入 Codex。
4. 打开一个测试项目或文件夹，确认 Codex 能创建任务。
5. 进入“接入 ModelPort”步骤，复用密钥页生成的配置，并完全重启桌面应用后验证新任务。

Windows 教程：

1. 从 OpenAI 官方 Windows 桌面应用页面下载安装器；同时提供官方 `winget install --id 9PLM9XGG6VKS -s msstore` 作为备用安装方式。
2. 安装并登录后，在应用内选择 Codex；普通用户默认推荐 Windows 原生环境，不把 WSL2 设为必选前置条件。
3. 若需要项目审查与 Git 功能，提示用户安装 Git；Node.js、Python、.NET SDK、GitHub CLI 归入“推荐工具”，不阻塞首次安装。
4. 打开一个本地测试项目，确认 Codex 能创建任务。
5. 进入“接入 ModelPort”步骤，使用 Windows 配置目录 `%USERPROFILE%\.codex`，完全退出并重启桌面应用后验证新任务。

进阶内容：

- CLI 和 IDE 扩展只放在文末“其他使用方式”，不与桌面版并列为新手推荐方案。
- Linux 不属于本期 Codex 安装教程验收范围；如后续需要，单独建立桌面 Linux 或 CLI 教程。
- 桌面应用登录、应用自身功能与 ModelPort API 接入分别说明，避免用户误以为 ModelPort 提供或修改 OpenAI 客户端。

维护规则：

- 不在正文写死 Codex 或桌面应用版本号，也不缓存官方安装包。
- macOS 与 Windows 下载地址均以 OpenAI 官方桌面应用文档为源；Windows 的 `winget` 包 ID同步官方说明维护。
- 页面提供“查看 OpenAI 官方桌面应用文档”和“Windows 专项说明”链接，站内教程只负责中文化与 ModelPort 接入说明。
- 每次桌面应用更新影响配置目录、登录方式或 Codex 入口时，必须重新验证两个系统的完整流程。

### 6.2 CC Switch 下载教程

内容范围：

- 只使用项目声明的唯一官方渠道：`ccswitch.io`、官方 GitHub 仓库和 GitHub Releases。
- 根据 Windows / macOS / Linux 显示下载提示、安装权限和首次启动说明。
- Windows 专门解释自定义协议 `ccswitch://` 的注册和浏览器弹窗授权。
- macOS 专门解释首次打开、安全提示和协议唤起。
- 安装完成后提供“返回 API 密钥页并一键导入”按钮。

维护规则：

- 下载按钮指向 `https://github.com/farion1231/cc-switch/releases/latest`，不把具体版本写死在正文。
- 可显示“当前推荐版本”徽标，但它必须来自站点设置或构建时常量；更新失败时退化为“最新稳定版”。
- 当前代码中的“建议 v3.20.1 或更新版本”应改成可维护字段或“最新稳定版”。

### 6.3 中转站配置教程

推荐以“选择客户端”开头，而不是先展示大量配置文件：

- Codex：选择支持 OpenAI/Responses 的分组 → 创建 Key → 使用密钥 → 下载/复制配置 → 完全重启 → 新建任务验证。
- Claude Code：选择 Anthropic 或适配分组 → 设置 Base URL 与 Key → 重启 → 测试。
- CC Switch：选择当前 Key → 一键导入 → 客户端页签启用供应商 → 确认导入 → 完全重启目标客户端。
- 其他客户端：复用 `UseKeyModal` 已生成的配置。

配置页必须显示三类信息：

- API 地址：基于站点当前 `api_base_url` 动态生成。
- API Key：仅在用户主动打开密钥操作后使用，不写入教程 HTML、查询参数、日志或埋点。
- 模型 ID：从当前分组模型目录读取，不在教程正文写死。

## 7. 交互与复用设计

### 7.1 从教程回到真实操作

教程不是配置生成器的另一份副本。文章中的主按钮应跳转到真实功能：

- “创建 API Key” → `/keys?action=create`
- “选择密钥并配置” → `/keys?guide=use-key`
- “一键导入 CC Switch” → `/keys?guide=ccswitch`
- “查看用量” → `/usage`
- “查看可用模型” → `/available-channels` 或模型广场

密钥页需要解析这些查询参数并高亮下一步；用户仍要主动选择 Key，避免页面自动读取或暴露密钥。

### 7.2 搜索

首期采用前端静态索引即可：

- 搜索标题、摘要、关键词、错误码。
- 结果显示所属分类和预计用时。
- 支持 `401`、`ccswitch`、`Windows`、`首字延迟` 等直接查询。
- 不把搜索词上传服务器，避免意外收集粘贴的 API Key。

### 7.3 阅读进度

- 使用浏览器本地存储记录已读教程，不同步服务端。
- 首页显示“继续上次阅读”。
- 首期可不做强制步骤锁定，用户始终可以跳转。

## 8. 建议的前端结构

```text
frontend/src/
├── views/user/GuideView.vue
├── features/guide/
│   ├── GuideHome.vue
│   ├── GuideSidebar.vue
│   ├── GuideSearch.vue
│   ├── GuideArticle.vue
│   ├── GuideOsTabs.vue
│   ├── GuideStep.vue
│   ├── GuideCallToAction.vue
│   ├── guideCatalog.ts
│   └── content/
│       ├── quick-start.zh.md
│       ├── codex-desktop-macos.zh.md
│       ├── codex-desktop-windows.zh.md
│       ├── codex-cli-advanced.zh.md
│       ├── ccswitch-install.zh.md
│       ├── ccswitch-config.zh.md
│       ├── codex-config.zh.md
│       └── troubleshooting.zh.md
└── utils/clientConfig/
    ├── codex.ts
    ├── claude.ts
    ├── ccswitch.ts
    └── types.ts
```

实施时应把 `UseKeyModal.vue` 内体量较大的配置生成逻辑逐步抽到 `utils/clientConfig`，让密钥弹窗和教程 CTA 使用同一来源，避免教程与实际配置漂移。

新增路由：

```text
/guide                  手册首页
/guide/:articleSlug     教程文章
```

路由需要登录，但允许管理员和普通用户访问；title/description 使用 i18n。

## 9. 内容维护方案

### 首期：随代码版本发布

- Markdown 与截图进入仓库，代码评审时一起检查。
- 每篇文章包含 `lastReviewedVersion`、`sourceUrls` 和关键词元数据。
- 当 Codex、CC Switch 或本站配置逻辑变化时，相关测试要求同步更新教程。

### 后续：后台草稿与发布

在确有运营需求后，再扩展当前 Pages 能力：

- 管理员 Markdown 编辑、图片上传、预览、草稿/发布状态。
- 版本历史、一键回滚、操作审计。
- 发布前链接校验和敏感信息扫描。
- `/guide` 只读取已发布版本。

不建议直接让后台写任意 HTML；继续使用 Markdown + DOMPurify 白名单。

## 10. 安全要求

- 示例统一使用 `<YOUR_API_KEY>`，禁止在 Markdown、截图、URL、埋点或错误日志中包含真实 Key。
- 一键导入深链只能在用户点击时临时生成，不预先渲染到 DOM，不持久化。
- 复制按钮不得把 Key 写入本地存储。
- 外部下载只链接官方来源，并标记“第三方软件”。
- 用户提交故障信息时提示遮蔽 Key、邮箱、请求正文等敏感内容。
- Markdown 继续使用 DOMPurify；若保留 iframe，限制来源并受 CSP 控制。

## 11. 分期计划

### P0：入口与最小闭环

- 新增 `/guide` 与顶部入口，位置在通知铃铛左侧。
- 完成手册首页、目录、搜索和移动端入口。
- 上线 6 篇核心内容：快速开始、Codex 桌面版 macOS、Codex 桌面版 Windows、CC Switch 下载、CC Switch 配置、常见故障。
- 从教程跳转密钥页、用量页和模型页。
- 将现有 `doc_url` 下沉为“更多文档”。

### P1：配置联动

- 密钥页支持教程查询参数和下一步高亮。
- 抽取并复用客户端配置生成器。
- 教程内加入 OS 标签、复制按钮、验证步骤和故障跳转。
- 补齐 Claude Code、Gemini CLI、OpenCode、Grok CLI。

### P2：运营维护

- 管理员草稿/发布、图片上传、版本历史和回滚。
- 失效链接检查、内容审阅时间提示。
- 仅记录匿名文章访问/完成事件；不记录搜索内容、配置内容或 Key。

## 12. 验收清单

### 导航与页面

- [ ] 桌面端“使用手册”位于通知铃铛左侧。
- [ ] 移动端顶部或用户菜单始终可进入手册。
- [ ] `/guide` 刷新、浏览器前进后退和直接访问均正常。
- [ ] 深色/浅色模式一致，目录和正文在窄屏不横向溢出。
- [ ] 键盘可访问入口、搜索、目录、OS 标签和复制按钮。

### 内容与配置

- [ ] Codex macOS、Windows 下载均只指向 OpenAI 官方页面，不托管安装包。
- [ ] CC Switch 下载只指向官方站点/仓库/Releases。
- [ ] macOS 与 Windows 的 Codex 桌面版下载、安装、首次启动、进入 Codex、接入 ModelPort 和新任务验证分别实测通过。
- [ ] Windows 网页安装器与官方 `winget` 备用安装方式至少各验证一次。
- [ ] 所有配置示例与 `UseKeyModal` 实际输出一致。
- [ ] CC Switch 一键导入成功、取消、未安装和协议未注册都有明确回退。
- [ ] 配置完成后包含“完全重启客户端 + 新建任务验证”。

### 安全与回归

- [ ] 页面源代码、DOM、URL、localStorage 和日志均无真实 API Key。
- [ ] Markdown 恶意 HTML、脚本、危险 URL 被拦截。
- [ ] 普通用户不能看到管理员专属内容。
- [ ] 顶部栏在常见宽度下不挤压余额和用户菜单。
- [ ] 原有公告铃铛、模型广场、语言切换和外部 `doc_url` 行为无回归。

## 13. 资料来源

- OpenAI 桌面应用官方文档：https://learn.chatgpt.com/zh-Hans/docs/app
- OpenAI 桌面应用快速入门：https://learn.chatgpt.com/zh-Hans/docs/quickstart
- OpenAI Windows 桌面应用专项说明：https://learn.chatgpt.com/zh-Hans/docs/windows/windows-app
- OpenAI Codex CLI 官方文档（进阶可选）：https://learn.chatgpt.com/zh-Hans/docs/codex/cli
- CC Switch 官方仓库：https://github.com/farion1231/cc-switch
- CC Switch 最新发行版：https://github.com/farion1231/cc-switch/releases/latest
