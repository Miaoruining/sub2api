# New API 模型卡片对齐验收

## 验收基准

2026-09-07，用户明确补充「按照我们本地源码即可」。最终基准为本地 New API 当前代码，**不是最初截图的旧版大卡片**。

- 来源：`../new-api/web/src/features/pricing/components/model-card.tsx`、`model-card-grid.tsx`、`model-billing-mode-badge.tsx`、`../new-api/web/src/styles/theme.css`。
- 实际参考渲染：`http://127.0.0.1:31337/pricing`，使用本地 `new-api/web/dist`。临时只读服务器提供两条隔离定价样本，不修改 New API 数据库。
- 实际产品预览：`http://127.0.0.1:31335/model-plaza`。
- 同宽度比较入口：`http://127.0.0.1:31335/qa/newapi-cards.html`，源码 `frontend/qa/newapi-cards.ts`。不进入生产构建、不发送模型请求。
- 实现截图：本任务浏览器工具内嵌截图「验收本地 New API 同款卡片的最终尺寸与排版」；工具没有返回可保存的截图文件路径，以上 URL 和固定样本用于复现。

## 状态、尺寸与证据

参考与适配组件均为中文、浅色、匿名、标准计价、1M 单位，使用相同的两个模型名称、分组、空简介和数值。参考实例的货币符号为 ¥；Sub2 保留实际美元计价的 $，没有进行汇率或余额修改。

参考浏览器约 1272 × 720 CSS px、devicePixelRatio=1；在实现的隔离页面使用固定 859px 三列网格，使双方卡片宽度都为 **275.6640625 CSS px**。两边页面外围导航和卡片坐标不同，不纳入本次「卡片」验收。截图工具返回 JPEG，显示时可能缩放；精确尺寸以可见 DOM 的边界框测量交叉验证，不以图片显示缩放推断 CSS 尺寸。

最终两张全视图截图在**同一次比较输入**中查看，卡片文字、边框、按钮和价格均可辨认；隔离页面本身是聚焦卡片的区域证据。较早尝试的截图 `clip` 存在倍率偏差，未将该裁剪结果作为通过依据。

最终测量一致：

| 项目 | New API | Vue 适配 |
| --- | --- | --- |
| 卡片宽高 | 275.6640625 × 222.25px | 275.6640625 × 222.25px |
| 桌面内边距 | 20px | 20px |
| 卡片圆角 | 22.4px | 22.4px |
| 标题 | 15px、700、ui-monospace 字体栈 | 相同 |
| 价格 | 14px、20px 行高、等宽加粗数值 | 相同 |
| 描述区域 | 桌面两行、最小 40px | 相同 |

## 发现与修正记录

1. 用户切换基准后，旧图标大卡、底部「立即体验」和纵向价格区不再适用。已替换为本地源码的横向头部、右侧详情/复制、简介、分组计费及单位元数据。
2. [P1，已修复] Sub2 原有 `.card-header` / `.card-footer` 全局样式给适配卡片额外添加了内边距和分割线。改为独立的 `np-card-header` / `np-card-footer`，再次实际渲染确认额外分割线消失。
3. [P2，已修复] 在没有性能统计时，源组件的单位元数据位于底部右列，初稿误排到新一行。改为与源码一致的自动网格排列。
4. [P2，已修复] 价格标签与数值使用 inline-flex 导致每行比源组件少 0.5px。改为相同的内联基线布局后，两边总高度从 220.75 / 222.25px 对齐为 222.25px。
5. 操作图标使用本地 New API 的 Lucide 图标资源，以遮罩继承真实主题前景色；不是手绘替代品。

## 五项视觉检查

- **字体**：沿用源组件的 `'Public Sans', sans-serif` 与等宽标题/价格字体栈，字号、粗细、行高和标题单行省略与源码一致。没有额外引入网络字体。
- **布局**：桌面 20px、手机 12px 内边距；图标容器 40/36px，图标 28px；网格 1/2/3 列，间距 12/16px；按源主题实际圆角值适配 Tailwind 3，未直接误用同名类。
- **颜色**：采用源主题的 foreground、muted、border、info、purple 色值；按量蓝色、按次紫色，没有遗留旧截图绿色圆点标签。
- **资源**：平台图标复用现有图标资源，Gemini 使用 LobeHub 原始彩色路径，操作图标来自 New API 已安装的 Lucide。均附来源许可。
- **内容**：无简介时使用与源代码一致的「暂无描述。」；能力标签仅读取明确元数据。无性能数据就不展示性能，未编造延迟、成功率、能力或价格。

## 功能和响应式检查

- 单元测试覆盖分组合并、实际倍率、免费零价、缺少定价、元数据文本转义、详情及复制事件；模型广场相关 41 项测试通过。
- 浏览器验证搜索、重置、复制成功提示、详情弹窗和实际分组价格表。
- 390 × 844 CSS px：隔离卡片宽 366px、内边距 12px、图标容器 36px，文档宽 390px，无横向溢出。临时视口已恢复。
- 改写组件和补齐资源的过程中，Vite 热更新曾短暂记录动态模块加载失败；文件齐备后页面恢复。最终重新加载检查没有新增 error/warn，旧热更新日志不作为成品运行结果。
- 前端类型检查、受影响文件 lint 和临时输出目录生产构建通过。没有修改锁文件、构建配置或运行中的后端。

## 有意保留的业务差异

- Sub2 继续按真实分组及用户专属倍率计算价格范围；多线路长价格允许换行，防止遮住操作按钮。
- 缓存创建价、长上下文阶梯、高峰/分时价格仍在原有详情表中，不伪装成 New API 的表达式计费。
- 源卡片没有「立即体验」按钮，最终适配也不保留旧截图版的接入弹窗。
- 只对齐卡片，未重建整页导航、登录、余额、用户数据或后台权限。
- 适配组件保留 New API / QuantumNous 的 AGPL-3.0-or-later 归属和许可文件；原有项目许可证没有被替换。

## 结论

无待处理的 P0/P1/P2 卡片差异。动态内容、美元符号及现有页面外围布局属于明确保留的业务边界，不宣称两套完整页面的每个像素一致。

final result: passed
