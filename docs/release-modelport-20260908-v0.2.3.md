# ModelPort v0.2.3 合并与发布验收

状态：代码与镜像已推送，正式部署完成。

## 发布版本

- 上游基础版本由 v0.2.2 升级至 v0.2.3；合并发布标签及其后版本号同步提交。
- 应用冻结提交：`e05c662a0600b80cef85ddc9fbe2bcaf8d2c415a`。
- 功能分支：`feature/gemini-anthropic-compat`，已推送至个人仓库 `fork`，未推送上游或主干。
- 生产镜像：`mrn666/sub2api:deploy-e05c662a0-amd64`。
- 镜像摘要：`sha256:d1a1c1e94737b26fd8ba006bde64fb4108ea5215588eba18f32e9f901fc34845`，推送与生产拉取一致。
- 本文是部署后的文档记录；文档提交不改变上述应用冻结版本。

## 合并范围

- 纳入上游白名单列修复迁移，处理相关 API 密钥与订阅页面报错；纳入 Ollama DeepSeek 输出上限、鉴权及账号测试模型下拉显示名修复。
- 保留 ModelPort 自动切组、同平台路由、实际分组扣费、智能/低价/稳定策略、模型广场双样式及 CCSwitch 等定制代码。
- 调整迁移集成测试的事务内测试数据结构，以适配定制迁移保留旧白名单列的事实；未修改生产迁移业务逻辑。
- 补齐既有智能路由与模型广场样式的接口契约测试预期；未借此改变线上默认配置。

## 本地验证

- 前端：269 个测试文件、1927 个用例全部通过；类型检查通过。
- 后端：service、handler 及子包、server、middleware、routes、repository、migrations 单元测试通过。
- 数据库集成：APIKeyRepo 完整测试套件及迁移 236 的三个场景通过，覆盖自动分组往返及路由策略数据库约束。
- Linux amd64 生产镜像构建成功，前端类型检查、语言键检查及打包通过。
- Git 差异空白检查通过。

## 数据保护

- 发布前备份目录：`/opt/modelport/backups/modelport-20260908-e05c662a0/`，权限 700。
- 包含约 15 MB 的 `database.dump` 及 `compose.yml`、`Caddyfile`、`config.yaml`；数据库归档经 `pg_restore -l` 校验通过。
- 13 个未删除分组在候选迁移前后及正式实例启动后的整行摘要均为 `178eaacec7c7ee0fff78b3ee0ba552b2`。
- 迁移 `236_group_model_allowlist_repair.sql` 已记账；生产回填条件匹配记录为 0。
- 未主动调整用户余额、密钥配置、分组价格、排序或授权；正常请求仍按原业务逻辑记账。
- 只替换应用容器，PostgreSQL 与 Redis 未重启，启动时间仍为 2026-08-26。

## 生产验收与切换

以下时间均为北京时间（UTC+8）。

- 候选实例在 `127.0.0.1:8081` 启动并通过健康检查。
- 13:20:28：账号活动请求数为 0 时，Caddy 切到候选端口。
- 排空守卫最初使用主机上源/目标端口 8080 统计，误包含候选容器内部端口连接，因此保守阻止替换；随后改为读取旧容器自身网络命名空间，仅统计本地 8080 的 ESTABLISHED 连接，确认 0 后执行替换。
- 13:29:06：新版正式容器启动，版本与冻结提交匹配，状态 healthy，启动窗口未发现 ERROR/FATAL/PANIC。
- 13:30:51：正式实例真实流式测试通过，账号活动请求数再次为 0，校验 Caddy 配置后热重载回正式端口 `127.0.0.1:8080`。
- 回切后，`modelport.top`、`api.modelport.top`、`www.modelport.top` 的 `/health` 均返回 HTTP 200 和 `{"status":"ok"}`。
- 13:32:51：候选容器自身连接归零后正常停止，退出码 0；保留容器及日志，没有删除备份或旧镜像。
- 最终仅正式应用、PostgreSQL、Redis 三个容器运行，均 healthy；磁盘剩余约 58 GB，可用内存约 2.9 GiB。
- 线上 API 密钥页、用户订阅页、订阅管理页加载正常，显示版本 v0.2.3；用户订阅页的无有效订阅为空状态，不作为报错。
- 模型广场保留升级前现场的 Sub2API 原版表格样式；卡片式能力保留，本次没有改变管理员当前全站样式选择。

## 真实模型测试及已知问题

- 使用现有管理员测试密钥，在服务器内存读取并发起极短 `gpt-5.5` Responses 流式请求；没有输出密钥或复制至本地。
- 稳定分组：候选与旧版对照均返回 HTTP 200、OK 文本和 `response.completed`；更新后的正式实例再次通过相同测试。
- 另一 GPT Pro 测试分组：候选与旧版对照均返回 HTTP 502。该现象在旧版同样存在，尚未进一步定位具体渠道根因；本次未调整该渠道。
- 验收不等同于全部模型和上游渠道健康，也不代表本次已完成所有自动故障切组与计费路径的真实端到端验证。
- 排空后热切换旨在降低影响，不承诺所有客户端网络与空闲长连接绝对不中断。

## 回滚准备

- 保留旧镜像 `mrn666/sub2api:deploy-f90298def-amd64` 及发布前配置、数据库备份。
- 如需回滚，先验证旧镜像对当前数据库的兼容性及候选健康，再按活动请求归零、连接排空流程切换，优先只回滚应用镜像。
- 不直接恢复数据库快照，避免覆盖发布后的正常请求、余额与账单；确需数据恢复时应另行确认恢复范围和停写窗口。
