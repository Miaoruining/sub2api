# ModelPort 抽奖功能生产发布记录

状态：代码与镜像已推送，正式部署及收尾验收完成。

## 版本与推送

- 应用冻结提交：`17732ecdacb93f73269f7d2948746f50dcca2b63`。
- 功能分支：`feature/gemini-anthropic-compat`，已推送至个人仓库 `fork`；未推送上游或主干。
- 基础版本：v0.2.3，本次不升级上游版本。
- 生产镜像：`mrn666/sub2api:deploy-17732ecda-amd64`，Linux amd64。
- 镜像摘要：`sha256:5a735c640e0a3138266c774af2de2447407dd0c6bd1d096a421b3f5f7515222c`；构建、推送及生产拉取一致。
- 本文记录部署结果，文档提交不改变应用冻结版本。

## 上线规则与默认开关

- 普通用户开放区间为北京时间每日 `[10:00, 11:00)`，每天一次免费抽奖，不扣余额。
- 门槛为累计有效人民币实际支付充值满 10 元；退款扣减，管理员加款不计入门槛。用户曾被管理员加款，不影响其真实充值达标后的资格。
- 七档结果为无奖、1、5、10、20、50、100 额度。奖励真实到账，与支付充值记录隔离，不计作充值或触发充值返利。
- 每日全站真实奖励发放上限为 100 额度，管理员与普通用户共享；预算不足的奖项转无奖且不重抽，预算耗尽后结束。
- 默认权重为 `[9552,400,30,10,5,2,1]`，合计 10000。普通参与页面不展示基础概率、剩余预算或预算不足时的规则变化。
- 顶部余额左侧增加醒目红色呼吸动画抽奖入口。
- 普通用户活动开关保持迁移默认关闭，本次发布未主动开启活动。
- 管理员独立重复参与开关保持默认开启：免充值、不限次数、不受普通活动开关与时间窗口限制，但仍受每日全站发放上限约束。

## 验证依据

- 上线前已完成抽奖服务、处理器、数据库集成、前端测试和类型检查；集成覆盖普通用户 1000 并发、管理员重复请求幂等、角色降级、预算约束与时间边界。
- 管理员功能阶段前端全量回归：273 个文件、1947 个用例通过。最终时间窗口修改：5 个文件、25 个前端用例及抽奖后端/数据库回归通过。
- 本次冻结版本生产镜像构建成功，包含前端类型、语言键检查与打包；镜像版本命令返回对应冻结提交。
- 候选与新版正式实例均 healthy；各自启动窗口 ERROR/FATAL/panic 匹配行数为 0。
- 使用既有管理员验收密钥，在服务器内存中发起极短 `gpt-5.5` Responses 流式请求；候选与正式实例均返回 HTTP 200、OK 文本和 `response.completed`。未输出密钥或复制到本地。
- 线上抽奖参与页正常加载，管理员状态显示全天可重复参与、免充值，抽奖按钮可用；未点击生产抽奖按钮、未制造验收奖励流水。
- 线上管理页确认普通活动关闭、管理员参与开启、每日上限 100，参与页顶部显示 10:00–11:00 文案与余额左侧红色入口。

## 备份与数据保护

- 发布前备份目录：`/opt/modelport/backups/modelport-20260909-17732ecda/`，权限 700。
- 数据库自定义格式归档 `database.dump` 为 19,444,337 字节，`pg_restore -l` 校验通过。
- 同目录保存发布前 `compose.yml`、`deploy.env`、`config.yaml`、`Caddyfile` 与分组摘要，仅留在生产服务器受限目录，不提交仓库。
- 发布前、候选迁移后及正式回切后，14 个未删除分组的整行摘要一致：`d929c2588871cc02007b2ac85898fa91`。
- 迁移 `237_daily_lottery.sql`、`238_lottery_admin_repeat.sql` 均已记账。
- 未导入本地测试数据库、临时管理员密码或测试抽奖记录；未主动调整用户余额、分组、渠道、支付或密钥配置。

## 低影响切换

以下时间均为北京时间（UTC+8）。

- 候选 `sub2api-candidate-17732ecda` 仅监听 `127.0.0.1:8081`，沿用生产配置、数据库与 Redis。
- 00:46:09，账号活动请求数为 0，验证 Caddy 配置后热重载至候选实例。
- 旧实例首次排空检查发现 11 条连接，守卫阻止替换；随后在旧容器自身网络命名空间确认本地 8080 的 ESTABLISHED 连接为 0 后才更新正式应用。
- 00:51:44，新版正式容器启动；Compose 仅更新 `sub2api`，使用 `--no-deps`，不重启 PostgreSQL 或 Redis。
- 正式实例通过健康、版本及真实短流测试。首次回切检查发现 1 个活动请求，守卫暂缓切换。
- 00:54:14，账号活动请求再次归零，Caddy 校验后回切新版正式端口 `127.0.0.1:8080`。
- 回切后 `modelport.top`、`api.modelport.top`、`www.modelport.top` 的 `/health` 均返回 HTTP 200 与 `{"status":"ok"}`。
- 候选首次收尾检查仍有 2 条连接，继续保留运行；00:56:45，其自身连接归零后正常停止，退出码 0，保留候选容器和旧镜像。
- 最终仅正式应用、PostgreSQL、Redis 三个容器运行，均 healthy。数据库与 Redis 启动时间仍为 2026-08-26，未在本次发布中重启。
- 最终抽奖流水数量为 0，默认开关未变化；Caddy 配置与发布前备份完全一致，使用正式 8080 端口。停止候选后，三个域名健康检查再次全部通过。
- 收尾时磁盘可用约 57 GB，内存可用约 3.0 GiB。

## 回滚与验收边界

- 保留旧镜像 `mrn666/sub2api:deploy-e05c662a0-amd64` 及发布前数据库、配置备份。
- 优先验证旧应用与当前数据库兼容性后，仅回滚应用镜像，沿用候选验证与排空切换流程。
- 不直接恢复数据库快照，避免覆盖上线后的正常请求、充值、余额和奖励记录；如确需数据恢复，另行确认停写窗口与恢复范围。
- 真实短请求验收不代表全部模型和上游渠道均健康；本次未扩展排查上次已知的另一 GPT Pro 分组 502 问题。
- 本地已覆盖抽奖写入与并发；生产验收为迁移、配置和页面只读检查，未执行真实抽奖。低影响切换不承诺所有客户端空闲长连接绝对不中断。
