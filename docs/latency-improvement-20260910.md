# 延迟优化与验收边界

## 当前证据

2026-09-10 发布分段诊断后，只读核对真实业务请求，未发送付费模型测试。近期 CallAI 两笔样本分别为：

| 样本 | 请求发送完成 | 收到首个响应字节 | 结果 |
| --- | ---: | ---: | --- |
| A | 19 毫秒 | 36,965 毫秒 | HTTP 200 |
| B | 85 毫秒 | 125,108 毫秒 | HTTP 524 |

时间均从本站上游调用开始计算，两笔事件有序且连接、写入事件各一次。A 复用连接，B 在 82 毫秒取得连接。它们的主要等待发生在发送完成之后；不能据此进一步区分供应商网关排队、模型处理和响应回程，也不能把供应商后台显示的生成耗时直接当作同一测量口径。

历史另有大请求写入阶段达数十秒的样本，不能用上述两笔否定那类异常。需要继续核对同一时段主机内存、网络及 HTTP 流控。诊断只记录慢成功请求和错误，因此这些日志不能直接用于计算全站成功率或无偏的首字分位数。

另一个供应商的 OpenAI Responses 请求出现 503 和五轮同账号退避，等待分别为 0.5、1、2、4、8 秒，累计 15.5 秒，不包括每次上游处理时间。这是现有显式五次重试配置；需要评估末尾重试挽回的成功请求后才能降低预算。不可把下述 Anthropic 修复声称为该 OpenAI 路径的修复。

## 本次代码改进

### 晚间补充：同笔请求对齐

2026-09-10 晚间继续只读核对，按供应商账号、北京时间、映射模型和输入/输出 Token 匹配。供应商界面没有可见请求 ID，以下仍需供应商后端按诊断中的上游 ID 最终确认。

| 供应商 / 时间 | 供应商总耗时 | 本站成功尝试耗时 | 诊断结论 |
| --- | ---: | ---: | --- |
| CallAI / 17:51:49 | 1.52 秒 | 65.350 秒 | 写完请求至首个响应头字节约 64.975 秒 |
| CallAI / 17:51:51 | 1.72 秒 | 43.352 秒 | 写完请求至首个响应头字节约 42.870 秒 |
| CallAI / 17:51:57 | 1.62 秒 | 101.945 秒 | 写完请求至首个响应头字节约 101.558 秒 |
| WAWAZZ / 17:55:27 | 47.50 秒 | 47.652 秒 | 差约 0.152 秒，这笔主要是上游慢 |

这些样本输入 114、输出 11 Token，供应商模型为 gpt-5.6-sol、Medium。CallAI 三笔上游正文长度 934 字节，客户端获取为 0 毫秒，写完请求分别在 0、82、0 毫秒；响应头后约 0.4 秒即完成。已排除这些样本的主要延迟来自本站连接池等待、写请求或聚合响应尾部。还不能区分供应商入口排队、内部转发和响应网络回程。

CallAI 第二笔在成功尝试前另有约 125.020 秒的 HTTP 524；最终使用记录中的 43.352 秒不包括这次失败。当前使用记录总耗时是单次 Service 转发耗时，不能等同完整客户端等待；同步请求也没有首字指标。Novaris 本轮没有新的成功匹配样本，不据其孤立记录更新归因。

另一个供应商的晚期重试之后虽观测到 HTTP 200，但尚未成功关联入账记录，不能算作完整成功或直接计算重试挽回率，因此没有调整重试次数。活连接历史重传计数在 25 秒实时窗口中没有增加，无法解释上述历史请求。

本轮没有上线新代码或改变流量配置。下面的 Anthropic 重试修复已完成本地验证与功能分支推送，尚未部署，不能宣称已经提速。

补充复核：WAWAZZ 在北京时间 19:02–19:05 连续五笔匹配请求中，本站首字只比供应商多 215–226 毫秒，总耗时多 210–232 毫秒。CallAI 在 18:23:51 的新流式样本中，供应商首字 / 总耗时为 4.54 / 15.13 秒，本站为 115.080 / 124.361 秒；HTTP 诊断确认写完请求 3 毫秒、首响应头 114841 毫秒。这说明异常也发生在流式请求，且不同请求的差距不恒定。

Novaris 的历史错误记录另显示，北京时间 16:29:48–17:10:53 有 18 条上游 503 记录且均含 overload 提示。这支持该时段出现过上游过载，不能视为 18 个独立用户、当前仍过载或失败率。尚无新的成功请求匹配。服务器本轮可用内存 2740 MB、Swap 使用 399 MB，正式容器没有新增重启或 OOM；仍未改生产配置。

19:30 左右新增对照修正了 WAWAZZ 的适用范围：19:27:48 一笔相同时间、模型与 Token 的记录，上游首字 37.89 秒、本站 90.026 秒；请求正文约 1 MB，16 毫秒时写完，82.275 秒才收到响应头。因此 WAWAZZ 也存在间歇性的额外等待，不能将先前五笔的约 0.2 秒差距推广到所有请求。另有输出 11961 Token 的长请求，两边总耗时均约 6 分 26 秒，本站首字仅多约 0.2 秒，不属于同一种异常。

从生产主机各发一次无密钥 HTTP/1.1、HTTP/2 模型列表请求，三家均返回 401：CallAI 首响应约 0.20–0.22 秒，WAWAZZ 约 0.22–0.23 秒，Novaris 约 0.67 秒。没有调用模型或产生付费测试；这只表明当时未认证入口响应较快，不能据此判断认证后的推理路径正常或排除间歇协议问题。未改生产协议、连接池或超时。

### Anthropic 重试预算修复

通用 Anthropic 转发与 Anthropic API Key 透传在特定配置组合下，同时存在 Service 内层五次尝试和 Handler 外层池模式重试。外层每次重新调用 Service 都会重置内层预算。

当池账号的状态码明确由外层同账号重试接管，且该路径确实支持返回 failover 错误时，只保留外层为重试所有者。管理员配置的重试次数继续决定预算，避免乘法放大。非池账号、未交给池模式的状态码及请求参数修复保留既有行为。

此修复主要影响启用了自定义错误码、并触发内外两层重试的 Anthropic 池账号。不能宣称所有正常请求都会提速，更不能宣称已解决三家 OpenAI 供应商的全部慢请求。

本地验证通过：Service 定向测试覆盖真实转发调用计数、池账号 429/503、非池五次尝试、自定义 400/404 和取消退避；Handler 定向回归覆盖外层重试预算、取消和已写出语义内容后的 failover 保护。两组分别运行 10.486 秒和 7.855 秒，`git diff --check` 通过。

## 下一步上线与验收

- 本轮代码先完成本地验证；生产只读排查，不重启、不调整模型、计费或账号重试配置。
- 要缩短发送完成后的等待，需取得供应商可用的备用入口或低延迟线路，在同模型、推理强度、输入与缓存规模下对照验证；没有等价替代线路时，不截断长请求来制造更低耗时。
- 用户补充的 `wawazz.xyz`、`sub.callai.one`、`api.novaris.com.cn` 与当前生产入口一致，不能当作新线路进行切换。
- 首字和总耗时必须与成功率、超时率一起验收，并区分内部重试次数和用户请求数。供应商请求 ID 可用时优先用它对齐，不仅按时间猜测。
- 下一次发布保留现有内存软预算和回退镜像。4 GB 主机曾在双实例期间发生资源不足，必须先验证容量，不能仅凭健康接口通过就并行启动两份生产负载。
