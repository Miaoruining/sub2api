# ModelPort 上游延迟诊断补丁发布记录

应用版本 `0.2.4`，冻结提交 `5b44ea7517f4c3e80b3def1b00045ef9b12151b7`。发布 `latency-5b44ea751` 于北京时间 2026-09-10 17:14:54 完成，最终状态 `finalized`。正式实例承接 8080，候选在连接全部排空后正常停止并保留。

本次新增普通 HTTP 出站分段诊断，记录连接获取、DNS/TCP/TLS、请求写入、首响应与首次读取响应体的时间。没有新增正文、认证头或完整 URL 日志，没有数据库迁移、路由、收费或限额规则变更。诊断口径见 [上游延迟诊断说明](upstream-latency-diagnostics.md)。

## 镜像与备份

- 镜像标签：`mrn666/sub2api:deploy-5b44ea751-amd64`。
- Registry index：`sha256:c0dfb9df31e5c42b5bb27ea794090f9287aeaca291762ef921807088a7a876c5`。
- 部署的 linux/amd64 manifest：`sha256:43dbcb09f8dce9bd73ebdb9ae47d6f1fd9d61919f60f0df21cd54ecf1d3654d3`。
- 构建时间：`2026-09-10T08:24:09Z`。
- 发布工具 SHA-256：`8d73cbf3523644270fd0e4eff1272f25baf3764e7453026fd0eb6eb0fc521b3a`，服务器路径 `/tmp/modelport-release-5b44ea751.py`。
- 备份及阶段记录：`/opt/modelport/deploy/modelport-releases/latency-5b44ea751`，包含数据库归档及配置备份，不进入 Git。
- 数据库归档通过 `pg_restore -l` 验证，大小 140,077,227 字节，权限 0600。

## 发布期间的资源异常与恢复

发布期间发生了主机内存耗尽和访问超时，不能宣称此次发布全程零影响。现场测得主机内存总量约 3913 MB，可用约 3 MB，原 2047 MB Swap 已满，负载约 48.78。Docker 管理命令及本地健康检查也超时，正式实例的联合健康/版本验收因此暂停，Caddy 当时仍指向候选。

恢复过程没有强停正在承接公网请求的候选实例：

1. 增加权限 0600 的 4 GiB 临时 Swap，路径为该发布目录下 `recovery.swap`。启用后本地及公网健康检查恢复 200。
2. 确认闲置正式实例没有活动连接后正常停止，仅在生产 Compose 的 `sub2api` 环境中加入 `GOMEMLIMIT=1GiB`。修改前另存 `compose.before-memory.yml`，并验证差异仅为新增这一行。
3. 重新启动正式实例，核验内存参数、固定镜像与提交、HTTP 健康。正式实例启动时间为 `2026-09-10T09:10:30.696980166Z`。
4. Caddy 平滑回切至正式 8080。候选仍有 30 条连接时，脚本保留实例并暂缓停止；连接归零后再次执行收尾成功。

`GOMEMLIMIT` 是 Go 运行时的软内存目标，并非 RSS 硬上限，仍需观察实际内存及 GC 对延迟的影响。没有额外设置会直接终止实例的容器内存硬上限。

临时 Swap 保持启用，没有写入 `fstab`。不得删除正在使用的 Swap 文件；后续应在低负载、内存余量充足且完成容量评估后单独安排回收。不能用增加 Swap 替代长期内存容量管理。

备用控制台显示过历史 OOM 记录，但最终检查的两个本轮容器均无 OOMKilled、RestartCount 为 0，不能把历史记录直接当作本轮容器被杀死的证据。新诊断代码审查未发现无界事件队列、正文缓存或无界日志积压；主机压力的具体来源仍需结合实际请求和连接池使用量定位。今后发布前应检查双实例期间的实际内存预算，不仅检查单实例健康。

## 验收

- 发布前的 HTTP 诊断定向测试及相关 race 测试已通过；镜像的 amd64 架构、版本和远端摘要已核验。
- 正式实例已处理真实业务请求并输出 `http_upstream_trace`，包含 CallAI 的成功样本；没有为验收产生付费模型探测。
- 最终正式应用、PostgreSQL、Redis 均 healthy。数据库及 Redis 启动时间与发布前一致，未重启。
- 11 个有效分组摘要始终为 `a59d4db8e550d2bbc5a88ad272ac0ca5`。
- 最终 Caddy 摘要与发布前一致，回到 8080，5080 配置未改。
- 独立本机公网检查：`modelport.top`、`api.modelport.top`、`www.modelport.top` 的 HTTPS `/health` 均返回 200，分别约 0.36、0.55、1.27 秒。
- 最终主机可用内存约 2132 MB；Swap 总量 6143 MB，已用约 457 MB。

健康恢复和诊断生效不等于所有上游问题已解决。真实日志仍有大量上游 503，需按请求和渠道区分重试次数，不能把出站尝试数当作用户请求数。某一采样窗口内单请求单账号最多观察到 6 次尝试，没有据此认定无限重试。供应商之间的延迟差及内存压力继续由既有监控跟踪，不自动调整流量路由。

## 回退与后续

上一应用镜像的 amd64 digest 为 `sha256:bf586e45f922ce5704ff1b83049e383fb896817caa56fe3996cb7038c7429c38`。若需回退，仅回退应用镜像并执行健康验收与连接排空，保留已配置的内存预算和恢复资源；不要直接用旧 Compose 覆盖新增的内存参数，不用旧数据库归档覆盖期间产生的消费与订单。

后续重点检查实际连接池占用、重试造成的请求放大、Go 内存及 Swap 趋势，并按分段诊断对齐供应商记录后再判断责任。
