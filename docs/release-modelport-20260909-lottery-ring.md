# ModelPort 前三次概率与八格动画生产发布

状态：代码与镜像已推送，生产部署和收尾验收全部完成。

## 冻结版本

- 应用提交：`ede90d8b5a2c482cf8cd283fe85486aacb9b3b28`。
- 功能分支 `feature/gemini-anthropic-compat` 已在个人仓库 `fork` 确认同一提交。
- 生产目标镜像：`mrn666/sub2api:deploy-ede90d8b5-amd64`，Linux amd64，基础版本 0.2.3。
- 本机生产镜像构建成功；版本命令返回上述完整提交。
- 最终发布摘要：`sha256:3aafac02af8df877fa0e92b2b5e1dd278fe70253416afa563fef743aba0ee22a`，上传结果和生产拉取一致。
- 本地构建含附加证明清单，导出后使用独立上传工具发布单架构镜像，因此发布摘要不同于本地构建索引摘要；应用配置摘要和冻结提交未变化。服务器 Docker 的镜像 ID 返回清单摘要，已按不可变发布摘要及应用版本验证。

## 验证与网络恢复

- 最新前端完整回归 274 个文件、1,966 项测试通过；前三次概率服务、处理器单元测试和抽奖数据库集成测试通过。
- 生产镜像构建包含前端类型检查、语言键检查及生产打包。
- 首次两次 Docker Hub 推送发生 EOF；用户恢复登录后，网络恢复但 Docker 上传长时间停滞。终止该上传客户端后，使用已有校验匹配的独立上传工具，经当前代理成功上传同一镜像。未改系统代理、节点或 Docker 配置。
- 用户已完成 UCloud 和服务器终端登录。只通过现有终端完成发布，未创建新登录凭据或扩大访问权限。
- 候选及正式实例均 healthy，版本返回冻结提交；各自启动窗口的 ERROR/FATAL/panic 匹配为 0。
- 迁移 `239_lottery_intro_draws.sql` 已记账。上线前 5 条旧抽奖记录全部保留，未回填为专属抽奖记录。
- 线上页面确认八个外围格、两个「下次再来」、中央按钮；管理页前三次概率显示精度正确，普通概率保持原配置。
- 正式回切后，`modelport.top`、`api.modelport.top`、`www.modelport.top` 的健康接口均正常。
- 未执行生产抽奖、未制造验收奖励流水；未修改线上活动开关、用户余额、分组及密钥。

## 备份与低影响切换

- 发布前备份目录：`/opt/modelport/backups/modelport-20260909-ede90d8b5/`，权限 700。数据库归档 19,648,613 字节，`pg_restore -l` 校验通过；同目录保存原 Compose、环境、应用与 Caddy 配置，均仅留服务器受限目录。
- 发布前、候选迁移后及正式回切前，14 个有效分组摘要均为 `d929c2588871cc02007b2ac85898fa91`。
- 候选 `sub2api-candidate-ede90d8b5` 仅监听 `127.0.0.1:8081`，使用生产既有配置；未导入本地数据库或临时管理员密码。
- 以下时间为北京时间：02:36:16，活动请求归零、代理校验通过后切到候选。
- 第一次旧实例排空检查仍有 2 条连接，守卫停止替换；连接归零后，02:38:49 仅更新 Compose 的 `sub2api`，使用 `--no-deps --no-build`。
- 02:39:48，正式实例健康、版本、日志、分组检查通过，活动请求归零后热回切正式 `127.0.0.1:8080`。Caddy 配置恢复与备份完全一致。
- PostgreSQL 和 Redis 的启动时间均保持 2026-08-26，未在本次部署中重启。
- 候选收尾首次检查仍有 6 条连接，继续保留运行，等待排空。
- 02:42:26，候选连接归零后正常停止，退出码 0，保留容器与日志。最终仅正式应用、PostgreSQL、Redis 三个容器运行，均 healthy。
- 停止候选后，三个生产域名健康接口再次全部正常。生产验收未新增抽奖流水或发放测试奖励。

## 开关与验收边界

前三次按全部历史流水累计，不给老账号重新补次数；保持每日全站 100 额度上限。普通活动保持关闭，管理员独立参与保持开启，未为了验收改变开关。本次生产验收不实际抽奖，不发起额外计费模型请求；写入和并发逻辑已在隔离测试中覆盖，页面及健康检查不代表全部上游渠道均可用。

保留旧镜像 `mrn666/sub2api:deploy-17732ecda-amd64` 与数据库备份。优先回滚应用，不直接恢复数据库快照覆盖上线后的业务数据。排空与热切换降低中断风险，不承诺所有空闲长连接绝对不中断。
