# ModelPort 前三次概率与八格动画发布准备

状态：发布准备完成，生产部署未执行。等待 Docker Hub 网络恢复及 UCloud 重新登录。

## 冻结版本

- 应用提交：`ede90d8b5a2c482cf8cd283fe85486aacb9b3b28`。
- 功能分支 `feature/gemini-anthropic-compat` 已在个人仓库 `fork` 确认同一提交。
- 生产目标镜像：`mrn666/sub2api:deploy-ede90d8b5-amd64`，Linux amd64，基础版本 0.2.3。
- 本机生产镜像构建成功；版本命令返回上述完整提交。
- 本地构建清单摘要：`sha256:93656e205fbdca3a8ad20fb39dd1f7adb937f781ca2dbb10bfb146459087a73b`。这是本地摘要，不代表镜像已上传成功。

## 验证与当前阻塞

- 最新前端完整回归 274 个文件、1,966 项测试通过；前三次概率服务、处理器单元测试和抽奖数据库集成测试通过。
- 生产镜像构建包含前端类型检查、语言键检查及生产打包。
- 两次 Docker Hub 推送均在 registry blob HEAD 请求发生 EOF；本机直接连接及显式使用当前配置的本地代理连接注册表均出现 TLS 连接中断。未更改系统代理、节点或 Docker 配置。
- 已打开 Chrome 的 UCloud 登录页并请用户重新登录。未获取服务器新终端连接，未执行本次生产备份、迁移或容器替换。
- 生产域名 `https://modelport.top/health` 检查返回 `{"status":"ok"}`。
- 未执行生产抽奖或修改线上活动开关、用户余额、分组及密钥。

## 恢复后的步骤

1. 重新推送目标镜像，核对远端摘要；恢复 UCloud 与服务器终端登录。
2. 读取当前生产版本和配置，备份数据库与部署文件，仅将备份保留在服务器受限目录。
3. 拉取候选镜像，使用回环候选端口验证健康状态、版本及 `239_lottery_intro_draws.sql` 迁移记账；不导入本地数据。
4. 按既有流程等待活动请求和旧连接排空，切换代理并只更新应用容器，不重启 PostgreSQL 或 Redis。
5. 核对三个生产域名健康状态、管理员前三次规则、八格页面及日志，保留旧镜像和备份作为应用回滚依据。

前三次按全部历史流水累计，不给老账号重新补次数；保持每日全站 100 额度上限和现有开关。优先回滚应用，不直接恢复数据库快照覆盖上线后的业务数据。
