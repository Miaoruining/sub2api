# OpenAI API Key 账号 HTTP/1.1 灰度试验

## 目的与边界

部分真实请求写入上游很快，但接收首响应头等待数十秒。该证据尚不足以区分上游入口、内部转发和回程，也没有证明 HTTP/2 是根因。本试验提供可按账号回退的连接协议选择，用真实流量验证本站侧调整是否有效。

仅当账号为 OpenAI 平台、API Key 类型，并且 `extra.openai_http_transport` 精确为字符串 `http1` 时，核心 OpenAI 转发使用 HTTP/1.1。未设置、未知值、OAuth 和其他平台保持原行为。

新协议复用现有 OpenAI 超时与连接池设置，通过既有协议标识隔离 H1/H2 客户端。请求内容、模型、计费、重试和响应数据处理不变。此开关不涵盖独立账号测试入口、WebSocket 或插件自有传输；验证协议必须查看真实请求，不能仅使用后台测试按钮。

## 灰度与回退

先发布默认关闭的代码，只对一个低流量 CallAI 账号开启，另一账号保持对照。保存原字段是否存在及原值，修改时仅更新这一字段，并投递 `account_changed` 事件刷新调度快照。禁止覆盖整个账号 extra。

上线前备份现有版本与配置，保留 `GOMEMLIMIT=1GiB` 和既有活动 Swap。候选通过健康、版本和资源检查后平滑切流，旧连接未结束时保留实例，不强制停止。若出现新协议连接失败、持续错误增长或资源恶化，优先恢复单账号字段并刷新快照；需要版本回退时沿用平滑发布流程，不恢复整库。

## 验收与效果口径

- 定向测试验证默认和 OAuth 隔离、超时策略保留、连接池隔离、TLS 实际协商 HTTP/1.1，以及 EOF 前首个流式数据块可读。
- 发布后以版本、健康检查和真实请求 `http_protocol` 确认实现已生效。
- 从用量表按固定时间窗统计首字中位数、P90、30 秒以上数量，并按模型、流式状态、输入/缓存量及可用的推理强度分层。另查错误尝试与重试，不能仅看成功请求。
- 慢成功与错误才记录的诊断日志不能计算无偏成功率或全量延迟分位数；用量耗时也不包含此前失败尝试。两账号流量并非随机分组，不凭一两笔快请求宣称因果或扩大灰度。
- 优先观察自然流量，不重放客户内容，不额外产生付费探测，不向供应商发送消息。

## 实施记录

生产前置检查：2026-09-10 15:45 UTC，原版本 `0a5c79ba6`，正式实例无重启/OOM，约 2.8 GiB 内存可用，内存软限制仍为 1 GiB。发布工具与仓库脚本 SHA-256 一致。

基线固定为 2026-09-10 09:45 至 15:45 UTC，保存在生产受限权限文件。计划灰度账号的有首字流式成功记录仅 4 条，对照账号 96 条，样本不均衡。具体上线版本、启用时点与观察结论须在实际执行后补记，当前记录不代表已上线或已证明提速。

代码定向验收已通过：服务层账号匹配与网关调用、插件路由回归、repository HTTPUpstreamSuite 与 H1 TLS/流式测试；`git diff --check` 通过。测试使用本机 Go 1.27.0，没有调用付费上游。

## 上线与初始观察（2026-09-11）

- 发布代码：`19d064dc8fd2c8ea6d78fcdf4f32728708711ad5`，已推送自有 fork 功能分支。
- 镜像：`mrn666/sub2api:deploy-19d064dc8-amd64@sha256:191e50cdd15f002c1569bf31b325f9d36bf992f978ae554e44e289f6057788bf`，平台 linux/amd64，镜像内完整提交号与构建日期已核验。
- 发布编号：`http1-19d064dc8`。原版本 `0a5c79ba6`，备份位于 `/opt/modelport/deploy/modelport-releases/http1-19d064dc8/`。
- 候选通过健康与版本检查；双实例时可用内存约 2.7 GiB，保留 1 GiB GOMEMLIMIT，无 OOM/重启。首次 promote 有 10 条旧连接时安全退出，之后连接自然结束，正式实例更新通过。
- 2026-09-11 **00:01:24.830718 +08:00**，仅账号 32 开启 `http1`；数据库单字段写入与 `account_changed` 事件在同条 SQL 内完成。已复核数据库及 Redis 调度快照均为 `http1`，账号 33 仍无该字段。
- 原字段备份：`/opt/modelport/backups/http1-account32-20260910.json`，原值不存在，权限 0600。回退应仅在现值仍为 `http1` 时删除这一字段，并投递账号变更事件，避免覆盖后续人工变更。
- 开启前两账号近半小时没有新的用量记录。配置同步成功尚不代表实际请求协议已观测到，更不代表已提速；需继续采集真实流量。

最终 `finalize` 成功：候选的 3 条连接自然结束后停止，Caddy 回到正式 8080。正式 StartedAt 为 `2026-09-10T15:59:03.41505659Z`，健康 healthy，RestartCount=0、OOMKilled=false，公网健康 200，可用内存约 2854 MiB。启用后初次检查两个 CallAI 账号均无新 usage，trace 也无新增，因此尚无协议实际请求或效果结论。既有 30 分钟监控已更新为本次灰度观察，并保留异常时仅恢复单字段的回退规则。


## WAWAZZ 单账号试验（2026-09-11）

用户要求 WAWAZZ 也启用一个试验。选择 WAWAZZ-6（账号 7），近六小时成功用量 19 条；对照账号 8 为 77 条，9/10 无近期样本。账号均为 OpenAI API Key 类型、active，原协议字段不存在。

- 启用时间：2026-09-11 00:22:26.570556 +08:00，仅账号 7 写入 `http1`，同条 SQL 投递 `account_changed`。数据库及 Redis `sched:acc:7` 已核实同步，8/9/10 保持默认；CallAI 32 的既有试验保持启用，33 仍为默认。
- 原字段备份：`/opt/modelport/backups/http1-account7-20260911.json`，原字段不存在，权限 0600；回退只删除仍为 `http1` 的该字段并刷新调度，禁止覆盖整个 extra。
- 固定基线：UTC 2026-09-10 10:22:01.379984 至 16:22:01.379984，SQL/统计保存在 `/opt/modelport/backups/http1-wawa-baseline-20260911.txt`（0600）。账号 7 以 gpt-6-astra 为主，8 以 gpt-5.6-sol 为主，必须按模型分层比较。
- 本次仅热更新配置，没有新部署或重启。正式实例 StartedAt 仍为 2026-09-10T15:59:03.41505659Z，healthy，RestartCount=0、OOMKilled=false，公网健康 200。
- 初始验收只确认配置和缓存同步，尚未获得启用后的真实 H1 请求证据，不能断言提速。已纳入同一 30 分钟监控，分别观察 Wawa 7 与 CallAI 32，出现明确不利影响时按账号独立回退。
