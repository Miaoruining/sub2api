# 2026-09-09 拼单功能生产发布

## 发布版本

- 应用提交：`dffa932b3`，版本 `0.2.3`，个人仓库分支 `feature/gemini-anthropic-compat`。
- 镜像：`mrn666/sub2api:deploy-dffa932b3-amd64`。
- 生产拉取摘要：`sha256:e0e029c5359606b23bb90d3cad7654c1bd384a6f121ec77a031e71b6365cae59`。
- 原镜像：`mrn666/sub2api:deploy-d57a0445f-amd64`，保留用于应急回退。
- 北京时间 22:39 将新流量切到候选实例，22:41 旧连接归零后替换正式应用，22:42 正式实例验证完成并回切 8080。

## 本次上线内容

- 管理员发布固定商品，首位用户余额付款后自动开团；满员通知管理员在 24 小时内绑定账号发货。
- 拼单号池独立于普通中转号池，复用原有 OAuth 绑定和账号维护页面。
- 拼团和服务有效期按天配置，服务从发货时开始计算。
- 用户使用专属 Key，按模型价格消耗个人拼团 $ 额度；Plus 有周期、5 小时和周额度，Pro 只设置本地周期额度，均有个人并发限制。
- 使用记录提供拼单来源筛选及未结算预占展示。
- 拼团价格页与服务端扣额使用同一份 Standard 价表；订阅额度展示按席位数计算的 `相当于 1/N Plus/Pro` 标识。

## 数据与切换保护

- 备份目录：`/opt/modelport/backups/modelport-20260909-pool-dffa932b3`，仅服务器受限访问；包含数据库、原部署配置及原镜像信息。
- 数据库备份 55,166,170 字节，`pg_restore -l` 校验通过。凭据和客户明细未进入仓库。
- `240_pool_orders.sql`、`241_pool_delivery.sql`、`242_pool_products.sql`、`243_pool_credits.sql` 执行成功，迁移记录由 289 增至 293。
- 发布前、候选和正式实例阶段，10 个有效分组的整行聚合摘要一致。
- Caddy 校验通过后使用普通热重载切流，保持文件权限；最终配置内容与备份一致。
- 正式容器通过 `docker compose up -d --no-deps sub2api` 更新；PostgreSQL、Redis 保持 healthy，启动时间仍为 2026-08-26。同机其他项目未调整。
- 候选实例连接归零后正常停止，保留容器及镜像。

## 验收结果

- 本轮重新执行数据库集成测试 `go test -tags integration ./internal/repository -run 'TestPool' -count=1`，通过。
- amd64 生产镜像构建成功，包含前端类型检查、国际化检查和生产打包。
- 候选及正式二进制确认版本为 `0.2.3 / dffa932b3`；健康检查通过，日志检查未发现 ERROR、FATAL 或 PANIC。
- `modelport.top`、`www.modelport.top`、`api.modelport.top` 的 HTTPS `/health` 均返回 200。
- 使用浏览器现有登录态验证大厅、商品管理、独立号池、价格页、拼单使用记录。相关商品、订单、号池、通知、价格和预占查询接口返回 200；匿名价格查询返回 401。
- 价格页加载 19 个模型，长上下文切换后相应输入及缓存价格翻倍、输出价格乘 1.5。
- 拼单使用记录正确选中“拼单”，展示“进行中与待核对用量”区域。
- 线上 `PoolOrdersView` 与 `PoolPricingView` 静态资源和本地构建逐字节一致；订单资源包含订阅份额标识代码。
- 观察期内存在大量普通 `/v1/responses` 的 403 业务限制记录，切换前同样存在；数据库错误分类均为业务限制。未将这些历史业务拒绝误报为拼单发布错误，亦未修改客户限额。

## 验收范围与后续配置

线上初始没有拼单商品、订单或独立号池账号。本次未把本地演示商品和模拟凭据发布到生产，未创建真实付款、未扣客户余额、未调用客户上游账号。

后台页面和查询链路已验收；余额扣款、并发成团、发货和额度结算由本地隔离数据库测试覆盖。真实 OAuth 账号绑定、真实模型调用及对应积分流水仍需在正式商品和测试账号就绪后做完整生产验收。没有线上商品卡片，因此订阅份额标识在本次生产验收中采用部署资源比对，未声称已在真实订单上展示。

## 回滚原则

优先回滚应用并保留数据库新增表，不恢复旧备份覆盖发布后的真实消费。原版本不具备拼单额度约束；若已经开始售卖或发货，不能直接回退旧版继续接收拼单请求，应先暂停受影响入口并评估数据及权限兼容性。
