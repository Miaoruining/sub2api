# 2026-09-09 抽奖补开与每日时段调整

## 授权与规则

用户明确要求：今天补开一次，14:00–16:00；以后每日 09:00–11:00，并开启普通活动总开关。全部时间使用北京时间。

- 2026-09-09 仅开放 [14:00,16:00)，不包含 16:00。
- 次日起每日 [09:00,11:00)，不包含 11:00；日期例外不会跨日延续。
- 每人每日一次。今天普通用户没有成功抽奖，因此使用原有当天机会补开，不清空流水、不重置概率计数、不增加预算。
- 保持实付充值满 10 元门槛、历史前三次概率、普通概率和每日全站 100 额度上限。
- 保持管理员例外开启。验收不进行生产抽奖，不发放测试奖励。
- 普通页当天显示补抽时间与次日起时间，不公开概率或预算。

## 版本与验证

- 应用提交：`9d6b817e0`；已推送个人仓库功能分支。
- 发布镜像：`mrn666/sub2api:deploy-9d6b817e0-amd64`，Linux amd64，版本 0.2.3。
- 上传摘要：`sha256:e0da578750a8b47e493aec08a9fcf0104dcfa85cd503830adaaeeda84b81f63c`。前端生产打包、类型检查和后端镜像构建成功。
- 后端抽奖 service、handler 单元测试通过。
- 隔离 PostgreSQL/Redis 抽奖集成测试通过，包含 14:00 接受、16:00 拒绝、重试复用结果和次日 09:00；原有充值、并发预算与管理员场景继续回归。
- 前端相关 4 个测试文件、27 项测试通过，含开场倒计时和截止后刷新失败时禁止继续抽奖。

## 发布记录

新版本已部署并开启普通活动，候选实例排空收尾完成。

- 发布前生产备份目录 `/opt/modelport/backups/modelport-20260909-makeup`，权限 700。
- 数据库备份 25,746,282 字节，`pg_restore -l` 通过；同时保留部署、环境、应用及 Caddy 配置。含凭据文件仅留服务器。
- 发布前普通活动关闭，管理员例外开启；当日 6 条流水均为管理员无奖。
- 原镜像 `mrn666/sub2api:deploy-ede90d8b5-amd64` 保留用于回滚。
- 只部署应用，不重启 PostgreSQL、Redis，不恢复或导入本地数据库。

### 切换与配置审计（北京时间）

- 候选 `sub2api-candidate-9d6b817e0` 在回环端口 8081 启动，镜像摘要与上传一致，healthy、版本正确，启动日志错误匹配为 0。
- 第一次代理热加载因候选配置复制时带入 0600 权限，Caddy 服务用户无法读取而失败。已恢复原配置及 0644 权限并重新加载；日志出现部分旧请求连接取消，不能宣称零中断。
- 后续复制代理配置显式保留原权限，并在加载前检查服务用户可读；改用不带 force 的 `caddy reload`。
- 当前并发较高，未强行终止旧实例；12:03:31 新请求切到候选，三个域名从服务器公网路径及回环 TLS 检查均正常。
- 本机普通域名访问遇到代理连接异常，DNS 返回 198.18.0.145；使用生产 DNS 对应地址直连、保留原域名与证书校验后正常。未改本机代理、DNS、hosts 或浏览器安全设置。
- 12:05:54，依据用户开启授权，通过数据库事务将普通开关 false 改为 true，同时保存完整配置前后值到 `lottery_config_audits`。管理员身份与管理页一致，管理员例外和概率不变；未创建凭据或模拟登录。最终两开关为 `t|t`。
- 旧实例连接由 1 等待至 0 后，12:07:26 仅替换正式 `sub2api` 容器。
- 12:08:25，正式实例 healthy、版本 `9d6b817e0`、启动错误匹配 0，确认开关 `t|t` 后回切 8080；Caddy 内容恢复与备份一致。
- 14 个有效分组整行摘要在本次发布前、候选和正式阶段均为 `b9330332d1837d9945d4fa2b0757093d`。
- 本机通过保留域名证书校验的直连检查，三个域名 `/health` 均返回正常；公开前端已加载新资源，抽奖页面代码含当日补抽识别与两项补抽文案键。
- 候选剩余 4 条连接继续等待至 0 后，12:12:14 正常停止，退出码 0，保留容器与镜像。最终正式应用、PostgreSQL、Redis 均 healthy；数据库与 Redis 启动时间仍为 2026-08-26。
- 候选停止后，正式域名直连健康检查再次通过。未执行实际生产抽奖，未改变历史记录或余额。

## 回滚

保留原镜像与备份。若需回滚，优先回滚应用而非恢复数据库，避免覆盖发布后的订单与奖励。普通开关已按用户要求打开，应用回滚至旧时段版本会使今天下午补抽失效，应先明确处理活动开关与补抽安排；不得直接将旧数据库快照覆盖到生产。
