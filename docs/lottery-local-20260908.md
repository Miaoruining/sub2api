# 每日抽奖：本地实现与验收

本轮范围：本地开发与测试，不部署生产环境。普通用户活动默认关闭，由管理员显式开启；管理员专属重复抽奖开关默认开启（见文末追加规则）。

## 已确认规则

- 北京时间（UTC+8）每天 10:00 开放，最晚当天 23:59:59 结束；每日一次免费机会，不累计，不扣余额。
- 累计有效人民币余额充值实付金额达到 10 元即可参加，与当前余额无关。管理员加款、兑换码、赠送、邀请返利和抽奖奖励不计入，也不会导致真实充值用户被排除。
- 只计已支付、已完成且有支付交易号的订单。已退款订单排除；部分退款按到账额度的退款比例扣减实付人民币金额。待退款订单保守暂停计入。
- 非人民币支付不跨币种累加；有币种快照时以快照为准。缺少币种快照的历史订单仅对明确的人民币支付方式回退，未知方式不计入。
- 中奖档位：1、5、10、20、50、100 额度，另有无奖结果。
- 每日总发放硬上限 100 额度。默认权重（万分比）依次为 `[9552,400,30,10,5,2,1]`，即无奖 95.52%、1 额度 4%、5 额度 0.3%、10 额度 0.1%、20 额度 0.05%、50 额度 0.02%、100 额度 0.01%。基础理论平均每 1,000 次发放 95 额度，不是固定发放量。
- 超出剩余预算的奖品转为无奖，不重新抽取；预算归零结束当天活动。
- 用户页不显示概率、奖池金额或内部预算变化规则，不作固定中奖率或保底中奖承诺。

## 入口和界面

- 顶部金额图标左侧增加红色“抽奖”入口，2.8 秒轻微呼吸动画。移动端保留入口，系统减少动态效果时停用动画。
- 用户页 `/lottery`：奖品、服务器校准的开放倒计时、资格提示、今日结果及最近 30 次个人记录。
- 管理页 `/admin/lottery`：立即生效的活动开关、次日生效的概率设置、日期筛选、当日发放总额、中奖分布和最近 100 条奖励流水。
- 规则变更和操作者记录在 `lottery_config_audits`；当日权重快照保存在 `lottery_days`，不因次日配置更改而改变。

## 后端与数据约束

- 新增迁移 `237_daily_lottery.sql`，新增独立活动开关、规则版本、每日预算、抽奖流水和配置审计表；不改既有支付履约逻辑。
- 用户接口 `GET /api/v1/lottery` 和 `POST /api/v1/lottery/draw` 经过登录、运行模式与面板限流中间件。抽奖请求仅需 `activity_date`，身份始终取自认证上下文。
- 管理接口 `GET /api/v1/admin/lottery`、`PUT /api/v1/admin/lottery/config` 受管理员中间件保护，处理器再次验证管理员角色。
- 使用数据库时钟判断日期和开放时间，避免系统时区配置影响。等待锁后再次核实日期，旧页面不能消耗新一天机会。
- 当日预算行锁串行保护跨实例并发；用户行锁保护余额；用户与日期唯一约束防重复；数据库 CHECK 限制每日已发放不超过 100。
- 密码学安全随机票号由后端生成；前端没有随机开奖逻辑。
- 余额更新、奖励流水、预算扣减和次数增加同事务提交；任一步失败全部回滚。无奖也生成唯一记录。
- 相同用户、同一天重试返回原结果；完成后即使活动关闭或跨天，原日期重试仍可取回结果。奖励成功后清理鉴权与计费余额缓存。
- 用户 DTO 采用字段白名单，不返回权重、剩余预算、随机票号、中奖分布或其他用户信息。
- 奖励流水独立记录余额前后值，不伪装成充值订单，不触发邀请返利，也不产生充值资格。

## 已完成测试

- 穷举全部 10,000 个随机票号，验证概率边界、总权重及理论发放量；验证预算不足直接无奖。
- 北京时间 09:59:59、10:00:00、23:59:59、次日 00:00:00 边界和不同服务器时区。
- 实付 10 元但到账仅 1.4 额度仍符合资格；仅余额被管理员增加不符合资格；退款、外币、订阅订单及未支付凭据排除。
- 40 个同用户并发请求只生成一条流水并增加一次余额。
- 1,000 名用户并发请求，仅发出累计 100 额度，预算、流水合计和余额增量一致。
- 随机源失败回滚；余额更新后的流水写入失败仍整体回滚。
- 超预算转无奖、无奖消耗当日次数、跨天重试、次日规则和配置审计。
- 用户与管理员处理器权限、认证身份绑定、日期校验及用户接口敏感内部字段隔离。
- 前端用户页所有状态、重复点击、响应丢失后的结果恢复、倒计时开放刷新、错误重试及内部概率信息隐藏。
- 管理页参数校验、整数权重提交、开关独立更新和失败后状态恢复；顶部入口位置、呼吸动画和减少动态效果支持。
- 最终前端全量回归 272 个测试文件、1,943 个用例通过，包含管理页新增测试。相关后端单元回归、数据库集成测试（含既有迁移 236 和 API Key 仓储回归）、类型检查、ESLint 与 Go vet 通过。

## 本地运行验收

- 最终镜像 `modelport-lottery:local-final`（linux/arm64）构建成功；仅替换 `sub2api-dev` 应用容器，PostgreSQL 与 Redis 保持原启动时间，未重启。
- 本地地址：`http://127.0.0.1:31333`；`/health` 返回正常，容器健康状态为 `healthy`。
- 应用版本为 `0.2.3-lottery-local`，镜像构建标识为 `lottery-local-working-tree`，属于本地预览而非正式发布。
- 迁移后的抽奖表已存在，默认活动关闭，抽奖流水为零。
- 使用重置后的本地管理员密码验证登录成功；用户抽奖状态接口和管理员抽奖接口均返回 HTTP 200。匿名访问用户抽奖接口返回 HTTP 401。
- 实际用户接口仅返回白名单字段，不含预算、权重或随机票号；当前状态为 `disabled`。
- 用户已自行登录。实际浏览器确认顶部抽奖入口位于余额左侧，背景为 `rgb(199, 45, 64)`，呼吸动画周期 2.8 秒；点击可进入 `/lottery`。
- 用户页显示六档奖励和无奖结果，未显示基础概率、剩余奖池或预算变化规则；关闭状态下抽奖按钮不可用。管理页正确显示默认关闭、发放 0、参与 0 和次日配置。
- 390 × 844 手机尺寸下，用户页及管理页均无整页横向溢出，红色入口继续显示；验收后恢复原窗口尺寸与深色主题。

## 登录后的样式回归

- 实际深色主题验收发现奖品区域仍为浅色。原因是 Vue scoped CSS 将 `:global(.dark) .lottery-stage` 编译为裸 `.dark`，既丢失后代选择器，也会误作用于主题根节点。
- 将完整后代选择器放入 `:global(...)`，只修正抽奖区域与奖品卡，不改变全站主题或业务规则。
- 新增 CSS 编译回归测试，确认深色后代选择器保留且不产生裸 `.dark` 规则；旧版本复现该问题。
- 本次抽奖相关 4 个测试文件、17 个用例和定向 ESLint 检查通过。
- 修正版本地镜像构建及更新成功（包含类型检查与正式前端打包），健康接口正常；实际浏览器复验深色区域背景和奖品卡颜色均正确生效，标题恢复清晰可读。
- 该样式修正阶段未开启活动、未更改概率、未产生抽奖或充值记录，也未部署生产。

## 本地环境保护

- 更新本地容器前已备份数据库：`/tmp/modelport-lottery-backup.Ze9iJG/database.dump`，目录权限 700，归档目录校验通过。
- 保留更新前本地镜像别名 `modelport-lottery:before-20260908`。
- 经用户明确授权，仅重置本地管理员密码；未修改线上账号。临时密码不进入仓库、镜像或本文。
- 本地实例不得用于真实支付或生产压测；后续生产上线仍需用户另行确认。

## 验收边界

测试验证的是应用数据库内的一致性和既有订单字段口径，不替代支付渠道对账。历史缺失币种或支付凭据的订单不会通过人工猜测自动补齐。预算保护并不保证所有奖品在一天内一直可抽取；100 额度奖品仅在当日尚未发放其他额度时有可能发出。

## 管理员重复参与：用户追加确认

- 管理页新增“允许管理员随时重复抽奖”，`admin_repeat_enabled` 默认开启，立即生效。适用于当前有效管理员账号；关闭后管理员恢复普通参与规则。
- 开启后，管理员免充值门槛、不限每日次数、不受普通活动开关和北京时间 10:00 开放限制。每日预算仍按北京时间自然日重置，管理员也不能用旧日期发起新抽奖。
- 奖励真实记入余额；管理员和普通用户共享原有每日 100 额度上限、奖项概率和超预算转无奖逻辑。管理员没有额外奖池，也没有保证中奖。
- 普通用户规则不变，管理员开关不能开启普通用户活动，也不能让普通用户绕过充值门槛或每日一次限制。
- 新增迁移 `238_lottery_admin_repeat.sql`，保留全部历史流水；原每日唯一约束改为普通抽奖的部分唯一索引，管理员流水标记 `admin_repeat=true`。
- 管理员每次主动抽奖需携带新的 UUID `request_id`；数据库以用户、日期和请求编号唯一约束去重。网络重试使用同一编号，返回原始结果；即使已发生后续抽奖、活动关闭或跨天，也不会重复加款。
- 开奖事务在锁定用户后重新检查真实角色，不信任客户端传来的管理员标志。专属开关变更进入配置审计。
- 用户页对获准管理员显示“管理员可重复参与”和“再抽一次”；普通用户看不到这类专属说明。未确认请求保留编号并显示“重试本次抽奖”。用户接口仍不返回概率、预算或随机票号。
- 管理页统计文字调整为“当日抽奖次数”，避免管理员重复参与后被误认为独立人数。
- 新增验证：管理员未充值且普通活动关闭、10:00 之前可参与；重复无奖仍生成独立流水；同一编号 40 并发只发奖一次；不同编号 120 次请求最多发出共享预算余额；关闭专属开关及角色降级后失去重复资格；旧日期重放不重复发奖。
- 本次数据库集成测试通过，保留原 1,000 名普通用户并发预算回归；前端定向 20 项用例、类型检查和 ESLint 通过。
- 最终前端全量 273 个测试文件、1,947 个用例通过；服务、用户及管理员处理器、服务器路由、中间件、仓储和迁移的相关后端单元回归全部通过。
- 更新前再次备份本地数据库至 `/tmp/modelport-admin-repeat-backup.JlLlbD/database.dump`（目录 700、文件 600、归档校验通过），旧镜像保留为 `modelport-lottery:before-admin-repeat-20260908`。不自动回删含新流水的数据库约束。
- 新镜像已更新到本机 `127.0.0.1:31333`，迁移 238 生效，应用健康；PostgreSQL、Redis 未重启，生产环境未改动。
- 本地浏览器实测：管理员无充值、普通活动关闭时可抽奖；连续两次主动抽奖分别生成独立流水，随机结果均为无奖（未调概率），余额不变，今日次数为 2、发放为 0。测试流水保留，未清除或伪造充值。
- 实际管理页关闭专属开关后，管理员抽奖按钮禁用、专属资格提示消失；重新开启后恢复重复参与。最终保持 `enabled=false`、`admin_repeat_enabled=true`，普通活动没有被一并开启。
