# 2026-09-10 拼单大厅与我的订阅页面发布

应用提交：`4dc66e83801c60733645789c9260e76cd7cab3e3`，版本 `0.2.3`。

本次包含商品卡与正在拼团列表重排、价格文字入口、单一通知铃铛、个人拼单详情迁移到「我的订阅」，以及大厅和用户发货通知的跳转提示。后端、数据库迁移、Dockerfile 和部署模板相对线上 `11adc378e` 没有变化。

## 发布前验证

- 本地相关回归测试 24 项通过；国际化完整性 3 项、TypeScript 和生产构建通过。
- 代码已推送到个人仓库 `fork/feature/gemini-anthropic-compat`。
- 生产备份目录：`/opt/modelport/backups/modelport-20260910-pool-ui-4dc66e838`，受限权限。保存数据库归档、原部署与 Caddy 配置、原应用容器配置。
- 数据库归档 85,196,557 字节，`pg_restore -l` 校验通过。凭据和客户明细不进入代码仓库。
- 发布前应用、PostgreSQL、Redis 均 healthy，三个生产域名 `/health` 返回 200。
- 10 个有效分组的整行聚合摘要已记录，供切流后核对。

## 发布与线上验收

- 镜像 `mrn666/sub2api:deploy-4dc66e838-amd64`，平台 `linux/amd64`。
- Registry index 摘要 `sha256:dfb2ab5088142ddbc12ec5ad0097bf509ea7e8112822e20f66ee0cdd36d26bb8`；amd64 manifest `sha256:d343ff84249ecb2e0225acb4b277e39bee384d0826606b4ad51c17455882e2ff`。服务器拉取摘要一致。
- 二进制确认 `0.2.3 / 4dc66e83801c60733645789c9260e76cd7cab3e3`，构建时间 `2026-09-09T17:09:40Z`。
- 北京时间 01:25:55 新流量切换到已验证的候选实例；旧应用连接归零后，通过 Compose `up -d --no-deps --no-build sub2api` 替换正式应用。01:27:16 正式健康与版本确认后回切 8080。
- Caddy 最终配置与备份一致；PostgreSQL 与 Redis 均 healthy，启动时间仍为 2026-08-26，没有重启。10 个有效分组摘要与发布前相同。
- 正式和候选应用检查时 ERROR/FATAL/PANIC 均为 0；三个域名 HTTPS `/health` 均返回 200。
- 使用现有登录态验证生产大厅 5 款正式商品的展开/收起、紧凑价格入口、到「我的订阅」的跳转，以及唯一通知铃铛中的公告与拼单通知入口。浏览器 error 日志为空。
- 验收账号的「我的拼单」显示空态，常规订阅区域保留。本次未创建付费订单、扣余额、上传本地演示数据或调用客户上游账号。已参与订单的完整展示、退款防重复和重试由本地隔离测试覆盖。

线上资源与发布镜像校验一致：

| 资源 | SHA-256 |
| --- | --- |
| PoolOrdersView-voRIk1UQ.js | c965b1d379aa3f44e6c110766d52f278e6a79e71d33d3374d2aae7014f0380e3 |
| SubscriptionsView-0eY1CkGg.js（用户页） | 99f55a0f67f16ba12538bc4474b099fcd5ffb11702a354e610c51c850f896a1e |
| AppLayout.vue_vue_type_script_setup_true_lang-soN39CdL.js（通知入口） | f5838a34b22ea0dbfb13b2e576895fa70d23bf48c077b040830aa9f5db9d252b |

## 回退与后续

保留上一镜像 `mrn666/sub2api:deploy-11adc378e-amd64` 及生产备份；本次无数据迁移，如需回退优先只回退应用，不用旧备份覆盖新产生的消费数据。

已知轻微文案问题：大厅无正在拼团订单时仍沿用“从下方选择商品”，而商品已移到上方；不影响购买或订单查询，后续统一调整为空间无关的提示。

候选实例收尾：服务端连接归零后正常停止，最终 `exited / 0`；候选容器、旧镜像和备份保留。
