# ModelPort Messages 上下文修复发布

## 发布范围

仅修复 Messages 兼容路径丢失首轮任务上下文的问题：取消尾部 12 条截断，禁止未经验证的 `previous_response_id` 续接替代客户端完整历史。原生 Responses/Codex 路径不变。不修改数据库结构、生产模型分组或 AirPaper 任务 46 的状态。

## 源码与制品

- 发布前实际生产提交：`d4297a9fb2af4a9549c61b508b627e8364ffb927`，已通过容器内二进制版本核实。
- 在隔离 detached worktree 上依次应用已推送的 `f42fff52e`、`e195b8d41656a78374f18b75e4946fe056dac7d7`。
- 集成提交：`fe9967e45bbe8427ef8a510b3a69fb543753f621`。
- 发布镜像：`mrn666/sub2api:deploy-fe9967e45-amd64@sha256:c6a0677fb5c20bfd76b9bdcb7992f7ef1a6dac0f32b4db2d8c14076e2c62a59d`。
- 回退镜像：`mrn666/sub2api:deploy-d4297a9fb-amd64@sha256:6d078522920ae593f561b244572beda1c9bb8544844861c72ec256fbe6819119`。
- 发布编号：`messages-fe9967e45`。

仅 2 个运行时代码文件、2 个测试文件和说明文档与生产基线不同。其他工作区未提交改动未纳入构建。现有功能分支上的两项源码修复及生产基线均已在用户 fork 保存；未将功能分支整体覆盖生产。

## 本地验收

- 在生产基线上 13 项 Messages 定向回归测试通过；全包测试未完成，不作为通过证据。
- `git diff --check` 通过，隔离工作区干净。
- 前端构建、i18n 完整性 3 项测试、Go amd64 发布编译通过。
- 镜像内二进制报告提交 `fe9967e45`。
- Docker Hub 镜像推送成功并返回以上 digest。

## 发布机制

使用已审查的分阶段工具 `/tmp/modelport-release-5b44ea751.py`。服务器与本地脚本 SHA256 均为 `8d73cbf3523644270fd0e4eff1272f25baf3764e7453026fd0eb6eb0fc521b3a`。

备份路径：`/opt/modelport/deploy/modelport-releases/messages-fe9967e45/`。候选监听 127.0.0.1:8081，验证后入口热切换，等待旧连接排空再晋升正式容器；不重启数据库、Redis，不触碰 5080 服务。

## 生产验收

备份、候选健康检查及入口切换通过。18 个模型分组摘要均为 `71fd8c2a8247dc6b689e2a3c6ada85ff`，数据库及 Redis 启动时间未变化。

入口切候选后，从 AirPaper 生产主机使用现有 ModelPort Luna 配置，经腾讯选择性 CONNECT 中转到 `/v1/messages`，共发出 3 次合成短请求，不使用论文或用户正文，无失败重试：

| 场景 | HTTP / 流式完整性 | 耗时 | 语义结果 |
| --- | --- | --- | --- |
| 首轮强制 `check_ready` | 200 / `message_stop`，`tool_use` | 4.28 秒 | 正确调用工具 |
| 完整首轮、工具调用、工具结果续轮 | 200 / `message_stop`，`end_turn` | 2.15 秒 | 正确返回首轮 `ORCHID-742` |
| 17 条历史，要求返回首轮测试码 | 200 / `message_stop`，`end_turn` | 3.47 秒 | 正确返回首轮 `ORCHID-742` |

测试脚本退出码 0。与修复前错误问候、无法找回首轮内容的对照相比，这两种合成复现已恢复。此结果证明短流式兼容请求的上下文保存修复有效，不等于完整论文长任务验收，也不保证上游永不故障。未重新运行任务 46。

正式容器晋升健康/版本检查通过，18 个分组摘要及数据库/Redis 启动时间不变。首次晋升因旧容器仍有 1 条连接而安全暂停，后续待连接自然结束后成功，没有硬停活动请求。

正式入口回切 8080 后再次执行同样 3 次流式合成请求，工具首轮 3.58 秒、工具结果续轮 10.57 秒、17 条历史 2.06 秒，全部 HTTP 200、`message_stop` 完整、两项首轮测试码语义断言通过，脚本退出码 0。本次发布后两轮合计 6 次请求，没有无限重试。

最终 `finalize` 成功：入口 `127.0.0.1:8080`、候选已停止、18 个分组摘要不变、数据库/Redis 未重启。第一次候选排空时仍有 5 条连接，保留运行并等待后再次成功；未强制中止连接。旧镜像与发布备份保留用于回退，未执行数据库回滚或垃圾清理。
