# ModelPort 2026-09-08 发布验收

状态：正式部署完成。

## 发布版本

- 应用提交：`f90298def7fe5b63ec31d0dbb28026140be104e0`。
- 官方基础版本：0.2.2，本次发布为 ModelPort 功能更新。
- 镜像：`mrn666/sub2api:deploy-f90298def-amd64`。
- 镜像摘要：`sha256:cb66841bc8fb80e7f31a36cc03772a07e5aa274149db410b8a7921f06d3be670`，推送与服务器拉取一致。
- 功能：智能/低价/稳定路由策略，模型完整分组价目，厂商四色排序，管理员切换卡片式/原版 Sub2API 样式。

## 数据与备份

- 发布前备份：`/opt/modelport/backups/modelport-20260908-f90298def/`，包含 `database.dump`、`compose.yml`、`Caddyfile`。
- 数据库备份约 14 MB，`pg_restore -l` 校验通过。
- 保留旧镜像：`mrn666/sub2api:deploy-c2ff330e5-amd64`。
- 新增密钥策略列迁移完成，52 条密钥记录默认 smart；固定分组密钥仍按固定分组调用。
- 候选启动前后 13 个未删除分组的整行摘要一致：`178eaacec7c7ee0fff78b3ee0ba552b2`。
- `ccmax 满血`、`Claude -满分` 等分组保留，没有重建或删除分组。
- PostgreSQL、Redis 连续运行 12 天，发布仅替换应用容器。

## 已完成验收

- 生产镜像构建成功，前端类型检查、语言键测试与打包通过。
- 候选实例 `sub2api-candidate-f90298def` 仅监听 `127.0.0.1:8081`，健康、版本和迁移检查通过。
- 管理员专用测试密钥的 `gpt-5.5` 短流式请求返回 HTTP 200、输出 OK，并收到 `response.completed`。未输出或复制密钥到本地。
- 北京时间 00:23:09，账号活动请求数为 0 时切到候选实例。
- 线上模型广场显示 30 个模型、11 个授权可见分组（数据库 13 个有效分组是不同统计口径）。
- Claude Fable 5 详情显示 Kiro、Claude -满分、ccmax 满血三个价目行；不满足调用资格的分组明确标记仅展示价格，未扩大调用权限。
- 管理员“系统设置 → 功能开关 → 模型广场展示样式”已出现两个选项。保留当前卡片式，未为了验收改变全站外观。
- 旧实例连接排空后，北京时间 00:26:10 完成正式应用容器替换；正式实例版本匹配、healthy，启动窗口未发现 ERROR/FATAL/PANIC。
- 北京时间 00:28:49，账号活动请求数再次为 0 时，代理切回新版正式端口 `127.0.0.1:8080`。
- 回切后 `modelport.top`、`api.modelport.top`、`www.modelport.top` 的 `/health` 均返回 `{"status":"ok"}`。
- 正式实例再次使用同一管理员专用测试密钥进行短流式验收，HTTP 200、OK 文本、`response.completed` 均通过。
- 北京时间 00:31:13，候选连接排空后正常停止，退出码 0；保留容器及日志，没有删除旧镜像和备份。
- 最终仅正式应用、PostgreSQL、Redis 运行，均 healthy。13 个分组的整行摘要仍与发布前一致。
- 最终磁盘剩余约 58 GB、可用内存约 2.9 GiB，Caddy 反向代理为 `127.0.0.1:8080`。

## 验收边界

短请求通过不代表所有模型或上游渠道全部健康。路由依据近期本进程观测和参考价格，非最低账单或最高稳定性保证。按活动请求归零和连接排空切换降低中断风险，不保证所有客户端网络及空闲 WebSocket 会话绝对不中断。
