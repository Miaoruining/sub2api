# 2026-09-09 抽奖余额同步与体验补偿

## 核账结论与修复

用户反馈中奖未到账。核对当时全部四笔中奖记录、账户余额、后续消费和余额缓存：原奖励均已入账，未发现数据库漏发。不能将后续消费后的余额下降认定为漏发。

前端存在可复现的问题：重新进入中奖页不会同步顶部余额；中奖后的账户刷新失败被静默忽略。此次修复：

- 服务端确认开奖后立即刷新账户，不等待动画完成。
- 重新打开中奖页、手动刷新状态时同步真实账户余额。
- 同步失败保留中奖结果并提示，后台轮询重试；不在客户端累加奖励，不重新开奖。
- 同一已同步结果避免后台重复刷新，管理员新的抽奖记录仍独立同步。

此问题可能解释用户的余额显示反馈；未取得用户现场截图，不能认定全部反馈均由此触发。后端原子加款与中奖流水同事务的逻辑保持不变。

## 独立体验补偿

经再次告知原奖励已到账，用户明确授权给已核实的四位中奖用户各额外赠送 1 额度，合计 4 额度。

- 已在生产事务中执行四笔原子余额增加，并同时写入独立 `admin_balance` 已使用流水。
- 以原中奖记录关联的唯一标记防重复，仅对成功插入标记的账户加款。
- 补偿与原中奖记录分开；不改变抽奖预算、概率、次数或实际支付充值资格，不触发邀请返利。
- 提交后复核补偿四笔、四人、合计四额度；原四笔中奖记录差额仍与奖励一致。
- 精确失效涉及账户的余额缓存；未清空 Redis。
- 数据库备份及补偿明细仅保留在生产受限目录 `/opt/modelport/backups/modelport-20260909-lottery-wallet`，不将客户标识或余额提交仓库。
- 备份文件 33,255,505 字节，`pg_restore -l` 验证通过。

## 验证与发布

- 应用提交 `d57a0445f`，已推送个人仓库当前功能分支。
- 前端相关 4 个文件、38 项测试通过；包含重进页面同步、失败后轮询恢复、动画结束前刷新、管理员连续抽奖、网络重试和时段规则。
- 前端类型检查通过。
- 后端抽奖 service、handler 的 `unit` 标签测试通过。
- 前端生产打包与 amd64 镜像构建成功，二进制版本为 `0.2.3 / d57a0445f`。
- 镜像 `mrn666/sub2api:deploy-d57a0445f-amd64` 已上传并部署，摘要 `sha256:9c083ce4c1fb78979aeea7dec72dc201f5de55e8939289f239753bb6058b5360`，生产摘要一致。
- 候选实例 8081 健康、版本和分组校验通过后，以不带 force 的 Caddy 热加载切换新流量。保持 Caddy 配置原权限，加载前检查服务用户可读。
- 等待旧实例入站连接从 6 降至 0，16:03:19（北京时间）仅替换正式应用。正式实例健康、版本正确，启动日志错误匹配为 0，随后流量回切 8080。
- 本轮 11 个有效分组整行摘要在发布前、候选及正式阶段一致；不变更抽奖时段、概率或开关。
- 三个公开域名健康检查通过。公开前端抽奖资源包含 `balanceSyncFailed` 和账户刷新逻辑。
- PostgreSQL、Redis 均保持 healthy，启动时间仍为 2026-08-26；Caddy 配置内容已恢复与备份一致。
- 原镜像 `mrn666/sub2api:deploy-9d6b817e0-amd64` 保留可回滚。未执行生产测试抽奖。
- 候选剩余连接从 5 等待至 0 后正常停止，退出码 0，保留容器和镜像；发布收尾完成。

## 回滚原则

只回滚应用，不恢复旧数据库覆盖新的消费和补偿。体验补偿是用户授权的独立加款，不随前端版本回滚撤销。
